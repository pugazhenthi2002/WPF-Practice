<UserControl x:Class="LoginPage.AnimatedTextBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LoginPage"
             mc:Ignorable="d" Height="274.705" Width="623.529">

    <Grid>
        <TextBox x:Name="mainTextBox" Background="Transparent" Foreground="Red" 
                 BorderThickness="0" VerticalContentAlignment="Center" Padding="20, 0, 0, 0"
                 GotFocus="MainTextBox_GotFocus"    LostFocus="MainTextBox_LostFocus"
                 PreviewKeyDown="PasswordTextBox_PreviewKeyDown"
                 PreviewTextInput="PasswordTextBox_PreviewTextInput"/>
        
        <Rectangle x:Name="roundedRectangle" RadiusX="20" RadiusY="20" StrokeThickness="2"/>
        <Label x:Name="placeHolder" Background="Transparent"    Foreground="Red"
               Content="Username" FontSize="24" MouseDown="PlaceHolder_MouseDown" 
               HorizontalAlignment="Left" VerticalAlignment="Center"
               Margin="20, 0, 0, 0"/>

        <Label Content="👁️" Foreground="Red" 
               HorizontalAlignment="Right" VerticalAlignment="Center"
               Name="viewPassLabel"/>
    </Grid>
</UserControl>
